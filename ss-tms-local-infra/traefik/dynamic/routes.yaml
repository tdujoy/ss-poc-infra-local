http:
  routers:
    # Example: route /equipment to equipment service when you add it to this compose/network
    # equipment:
    #   rule: "PathPrefix(`/equipment`)"
    #   service: equipment
    #   entryPoints:
    #     - web
  services:
    # equipment:
    #   loadBalancer:
    #     servers:
    #       - url: "http://equipment:8080"
